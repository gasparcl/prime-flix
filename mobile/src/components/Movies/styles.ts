import {StyleSheet} from "react-native"

export const styles = StyleSheet.create({
    container: {
        marginTop: 16,
        alignItems: "center",
    },
    cover: {
        width: 112,
        height: 150,
        borderRadius: 4,
        justifyContent: "flex-end",
        overflow: "hidden",
        marginRight: 6,
    },
})
